#include "switch.h"

/* Initialize the switches on port H */
void openSwitch(void) {
	/* nothing to do here */;
}

/* read port H switch SW5 (bit 0) */
int getSwitch(void) {
	/* will later on be replaced by input from stdin */
	return 1;
}
